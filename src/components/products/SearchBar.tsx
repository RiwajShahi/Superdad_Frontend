const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search products..."
      className="w-full md:w-[300px] px-4 py-2 border border-border rounded-lg bg-background"
    />
  );
};

export default SearchBar;